import styled from "styled-components";

const DropdownButton = styled.button`
  display: block;
  margin-top: 10px;
  border: none;

  svg {
    width: 30px;
    height: 30px;
  }
`;

export default DropdownButton;
